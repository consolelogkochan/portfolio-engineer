<?php

return [
    /*
    |--------------------------------------------------------------------------
    | フォーム表示から送信までの最低待機秒数
    |--------------------------------------------------------------------------
    | これ未満で送信されたリクエストを時間トラップで弾く。
    | 人間が入力するには数秒かかるが、ボットは即送信する傾向がある。
    */
    'min_seconds' => (int) env('SPAM_MIN_SECONDS', 3),

    /*
    |--------------------------------------------------------------------------
    | メッセージ本文に含められる URL の上限数
    |--------------------------------------------------------------------------
    | これを超える URL が含まれるリクエストをスパムと判定する。
    */
    'max_urls' => (int) env('SPAM_MAX_URLS', 2),
];
