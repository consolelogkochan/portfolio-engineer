<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 最大幅（px）
    |--------------------------------------------------------------------------
    | この幅を超える画像は、アスペクト比を維持しながら縮小する。
    | 元画像がこの幅以下の場合は縮小しない（拡大もしない）。
    */
    'max_width' => 1200,

    /*
    |--------------------------------------------------------------------------
    | WebP 品質（0〜100）
    |--------------------------------------------------------------------------
    | Intervention Image が WebP 変換時に使用する品質値。
    | 80 は視覚的劣化を抑えつつファイルサイズを削減するバランス値。
    */
    'webp_quality' => 80,
];
